# Sidecar only: Discord Gateway → POST /api/discord/ingest
# Do not run `next build` / `next start` on this service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.gateway"

[deploy]
startCommand = "npx tsx scripts/discord-gateway.ts"
restartPolicyType = "always"
